/*
 * The same poster as `/`. The image is attached to a route, not to the site,
 * and this page is what `/` becomes once the draw is over — so without its own
 * copy a link shared on WhatsApp would lose its preview the day the flag flips.
 */
export { default, alt, size, contentType } from "../opengraph-image";
